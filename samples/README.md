# Judge / reproducibility bundle

After running the pipeline with competition `input/` CSVs:

```bash
pip install -r requirements.txt
python run_pipeline.py --validate
```

## Committed samples (no raw PII)

| File | Description |
|------|-------------|
| `ceiling_validation_summary.json` | Walk-forward ceiling metrics by model (regenerated by `08_ceiling_validation.py`) |
| `genai_transparency.example.jsonl` | Schema for GenAI audit log |

## Full artifacts (local, gitignored)

| Path | Description |
|------|-------------|
| `output/AI_ACES_predictions.csv` | Submission predictions |
| `output/ai_aces_budget_allocations.csv` | WP budget allocation |
| `output/ceiling_validation_report.csv` | Full ceiling validation |
| `data/genai_transparency.jsonl` | Live GenAI log (append-only) |
| `data/outlet_intelligence.db` | Web app database |

## 10-minute verify

1. Place CSVs in `input/` (see root README).
2. `python run_pipeline.py --validate`
3. `python app/app.py` → open dashboard, one outlet XAI page.
